# 🌿 AI-Powered Plant Disease Detection

A Streamlit web app to classify plant diseases from leaf images.

This project uses a Deep Learning (CNN) model trained on the **Kaggle PlantVillage dataset**. The Streamlit app serves as the user interface for uploading images and getting predictions.


*  Streamlit UI built.
*  Model training on Kaggle (In Progress).
*  Integration of the final model into the app.

## How to Run
1.  `pip install -r requirements.txt`
2.  `streamlit run app.py`